# 通用 AI 启动提示词

将下面内容发送给任何能够读取项目文件的 AI：

```text
请读取 .ai-spec/AI-START.md，并严格按其中的启动协议执行。
先定位项目根和规范根，完成只读体检，识别项目阶段、项目类型和当前 AI 能力，按上下文路由加载最小规则集，然后输出 AI Spec 启动报告。
仅启动规范不代表授权修改业务代码；不要覆盖已有 AI 配置，不要猜测业务规则，不要跳过安全和验证要求。
```

如果规范直接位于项目根，将路径改为 `AI-START.md`。

